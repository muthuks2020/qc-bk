# ═══ Appasamy QC API — STAGING (AWS RDS) ═══
# Copy this file to .env for staging deployment

FLASK_ENV=staging
FLASK_DEBUG=true
SECRET_KEY=change-this-to-a-strong-random-key

# Auth
API_SECRET_TOKEN=staging-token-change-me-2026

# Database — AWS RDS (ap-south-1)
DB_HOST=report-db.ap-south-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=appasamy_rpt
DB_USER=XXXXXXX
DB_PASSWORD=XXXXXXX
DB_SCHEMA=qcapp

# File Uploads
UPLOAD_STORAGE=local
UPLOAD_DIR=./uploads

# Integrations — OFF for staging
ODOO_ENABLED=false
EMAIL_ENABLED=false
SCHEDULER_ENABLED=false
AUTH_ENABLED=false

# CORS — add your staging frontend URL
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Logging
LOG_LEVEL=INFO
LOG_FILE=./logs/app.log
